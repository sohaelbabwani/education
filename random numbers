# This program uses the randint() function from the random module to generate a random integer between 1 and 1000.
//code starts here
// Discuss
//code starts here
#Code starts here and where else.
from random import randint
x = randint(1, 1000)
print(x)


